<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="VirtualKeyboard.SettingsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Настройки"
    PrimaryButtonText="Сохранить"
    CloseButtonText="Отмена"
    DefaultButton="Primary"
    PrimaryButtonClick="SaveButton_Click"
    CloseButtonClick="CancelButton_Click">

    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="700">
        <StackPanel Spacing="24" Padding="8">
            
            <!-- Keyboard Layouts Section -->
            <StackPanel Spacing="12">
                <TextBlock 
                    Text="Языковые раскладки" 
                    FontSize="16" 
                    FontWeight="SemiBold"/>
                
                <TextBlock 
                    Text="Выберите раскладки, которые будут доступны при переключении языка" 
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    TextWrapping="Wrap"
                    Margin="0,0,0,8"/>
                
                <StackPanel x:Name="LayoutsPanel" Spacing="8">
                    <!-- Чекбоксы раскладок будут добавлены программно -->
                </StackPanel>
                
                <Border 
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="12"
                    Margin="0,8,0,0">
                    <StackPanel Spacing="4">
                        <TextBlock 
                            FontSize="12" 
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                            <Run Text="ℹ️ "/>
                            <Run Text="Переключение между языками осуществляется кнопкой 'Lang'"/>
                        </TextBlock>
                        <TextBlock 
                            FontSize="12" 
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                            <Run Text="💡 "/>
                            <Run Text="Необходимо выбрать хотя бы одну раскладку"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Default Layout Section -->
            <StackPanel Spacing="12">
                <TextBlock 
                    Text="Раскладка по умолчанию" 
                    FontSize="16" 
                    FontWeight="SemiBold"/>
                
                <TextBlock 
                    Text="Выберите раскладку, которая будет активна при запуске приложения" 
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    TextWrapping="Wrap"
                    Margin="0,0,0,8"/>
                
                <ComboBox 
                    x:Name="DefaultLayoutComboBox"
                    HorizontalAlignment="Stretch"
                    SelectionChanged="DefaultLayoutComboBox_SelectionChanged"/>
                
                <Border 
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="12"
                    Margin="0,8,0,0">
                    <StackPanel Spacing="4">
                        <TextBlock 
                            FontSize="12" 
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                            <Run Text="💡 "/>
                            <Run Text="При запуске приложения будет установлена выбранная раскладка"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
            
            <!-- Keyboard Scale Section -->
            <StackPanel Spacing="12">
                <TextBlock 
                    Text="Размер клавиатуры" 
                    FontSize="16" 
                    FontWeight="SemiBold"/>
                
                <TextBlock 
                    Text="Измените размер виртуальной клавиатуры от 80% до 120%" 
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    TextWrapping="Wrap"
                    Margin="0,0,0,8"/>
                
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Slider 
                        x:Name="ScaleSlider"
                        Grid.Column="0"
                        Minimum="80" 
                        Maximum="120" 
                        Value="100"
                        StepFrequency="5"
                        TickFrequency="10"
                        TickPlacement="BottomRight"
                        ValueChanged="ScaleSlider_ValueChanged"
                        VerticalAlignment="Center"/>
                    
                    <TextBlock 
                        x:Name="ScaleValueText"
                        Grid.Column="1"
                        Text="100%"
                        FontSize="20"
                        FontWeight="SemiBold"
                        MinWidth="60"
                        TextAlignment="Right"
                        VerticalAlignment="Center"/>
                </Grid>
                
                <Border 
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="12"
                    Margin="0,8,0,0">
                    <StackPanel Spacing="4">
                        <TextBlock 
                            FontSize="12" 
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                            <Run Text="💡 "/>
                            <Run Text="Изменение размера вступит в силу после перезапуска приложения"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
            
        </StackPanel>
    </ScrollViewer>
</ContentDialog>